class Inside {

  constructor({canvas, reference}) {
    this.canvas = canvas;
    canvas.height = reference.height;
    canvas.width = reference.width;
  }

  render({level, player}) {
    //
  }

}
